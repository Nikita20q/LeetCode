Map<String, String> map = new HashMap<>();
        for (List<String> data : knowledge) {
            map.put(data.get(0), data.get(1));
        }

        StringBuilder sb = new StringBuilder();
        int left = 0;
        while (left < s.length()) {
            if (s.charAt(left) == '(') {
                int right = left + 1;
                while (s.charAt(right) != ')') right ++;
                String temp = s.substring(left + 1, right);
                sb.append(map.getOrDefault(temp, "?"));
                left = right + 1;
            } else {
                if (left < s.length() && s.charAt(left) != ')' && s.charAt(left) != '(') sb.append(s.charAt(left));
                left ++;
            }
        }
        return sb.toString();
    }
}